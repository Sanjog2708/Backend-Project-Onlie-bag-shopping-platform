<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display'] relative">
    <nav class="w-full fixed  flex bg-pink-100 justify-between px-5 ">
        <h3 class="text-xl mt-3">Scatch</h3>
        <%var loggedin = typeof loggedin !== "undefined" ? loggedin:true %>
        <% if(loggedin){ %>
            <div class="flex gap-4 text-lg py-3">
                <a href="">Shop</a>
                <a href="/cart">Cart</a>
                <a href="">My Account</a>
                <a class="text-red-600" href="/users/logout">Logout</a>
            </div>
        <% }  %>
       
    </nav>